#!/usr/bin/env bash
# claude-review — wrapper script
# Usage: claude-review --pr https://github.com/owner/repo/pull/123

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec node "$SCRIPT_DIR/claude-review.js" "$@"
